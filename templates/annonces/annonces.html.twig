{% extends 'base.html.twig' %}
{% block title %}{{ 'navbar.dogs'|trans }}{% endblock %}
{% block pageTitle %}{{ 'navbar.dogs'|trans }}{% endblock %}

{% block body %}
    <div class="row mx-auto">

        {% for annonce in annonces %}
            <div class="card   border-dark pt-2 m-2" style="width: 18rem;">
                <h5 class="card-title">{{annonce.title}}</h5>
                <img src="{{annonce.picture}}" class="card-img-top picture-height-250 rounded-2" alt="...">
                <div class="card-body">
                    
                    <a href="/fiche/{{annonce.id}}" class="btn btn-primary">{{ 'annonces.details'|trans }}</a>
                </div>
            </div>

        {% endfor %}

    </div>

{% endblock %}